## Backlog – Layered Modular SDK

### High priority
- Usage normalization module and adoption across all providers.
- Capability registry audit and completion for all models in catalog.
- Standardize streaming `on_usage` emission across adapters.

### Medium priority
- Additional built‑in tools (JSON coercion helpers).
- Optional richer metrics sinks examples.
- Example local/on‑prem adapter scaffold.

### Low priority
- Mypy typing strictness for provider adapters.
- Retry policy configurability surface.
- Tooling to snapshot capability registry and diff across releases.


